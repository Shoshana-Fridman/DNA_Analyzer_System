//
// Created by mby on 15/07/2020.
//


#include "fileReader.h"

#include <fstream>

/*DnaSequence fileReader::Read(){
    std::ifstream file_dna;
    file_dna.open("file_dna.txt");
    std::string line_dna,dna_str;
    if(!file_dna.is_open()) {
        throw;
    }
    while(getline(file_dna, line_dna))
        dna_str+=line_dna;
    file_dna.close();
    return DnaSequence(dna_str);
}*/

